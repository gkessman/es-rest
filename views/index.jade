extends layout

block content
	h1 Backbone App

	div#initialContent
		= appData

	div.container

	script(id="studenttemplate", type="text/x-handlebars")
		<a href="#" class="name">{{name}}</a>

	script(id="morestudenttemplate", type="text/x-handlebars")
		<h2>{{name}}</h2>
		<p>Email Address: {{email}}</p>